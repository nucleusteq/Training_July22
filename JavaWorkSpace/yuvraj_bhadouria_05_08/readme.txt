1.SpringBootTeacherApplication is simple creation of CRUD operation in spring boot while using hibernate to connect Database.
	findings:
	a)Hibernate is a very usefull framework to remove the work of sql queries as it implements and performs CRUD operation on its own 
		using the JPA.
	b)Spring boot enable us to create a easy web application using the servlet with a easy to understand flow of code using the annotations.

2.Vending Machine is a application of real life vending machine which accepts money from the user and return it with a snack and money if 
	the user over pay for the product.it also keeps a track of quantity of product. 