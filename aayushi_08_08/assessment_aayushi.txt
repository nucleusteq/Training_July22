1) 2D Array
2) char
3)  int a[][] student = new int[m][n]; m= no. of rows , n = no. of columns.
4)
5) Checked exception- ClassNotFoundException, IndexOutOfBound
   Unchecked Exception - Runtime exception
7) The Throwable class

8)  int a[][] student = new int[m][n];
    for(int row = 0; row < student.length; row++)
		{for(int column = 0; column < student[row].length; column++)
				student[row][column] = in.nextLine();
			}
		}

9) Serialization can be used.
10) Set/HashSet stores unique data
11) collection.sort()
12) By using methods like Thread.notify(), Thread .wait()
13)  implements Runnable  or extends thhread



coding ques.


class Weather implements Runnable{

    public void run(){
       
        String arr[][]={"Raining","Sunny","Cold"};
        Weather weather= newWeather();
        int index = weather.nextInt(arr.length);
        System.out.println(Thread.currentThread().getName()+" Weather:"+arr[index]);
        System.out.println();

    }
......