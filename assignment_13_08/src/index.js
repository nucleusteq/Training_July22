import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';
import App from './App';
import reportWebVitals from './reportWebVitals';

const ayush = ReactDOM.createRoot(document.getElementById('ayush'));
ayush.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);

reportWebVitals();
